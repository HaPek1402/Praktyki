<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title><%= yield(:title) %></title>
    <%= stylesheet_link_tag 'pdf' %>
    <%= javascript_include_tag 'application' %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <h1>HHHH </h1>
    <div style='text-align:center;'><img src='/public/logo.png' width='100' height='100'><h1>My Blog</h1></div>
    <div id="content">
        <h1>LLLLLLLLLL</h1>
        <h1><%= @article.title %></h1>
        <p><%= @article.body %></p>
        <table>
            <thead>
                <tr>
                <th>Commenter</th>
                <th>Comment Body</th>
                </tr>
            </thead>
            <tbody>
                <% @comments.each do |comment| %>
                <tr>
                    <td><%= comment.commenter %></td>
                    <td><%= comment.body %></td>
                </tr>
                <% end %>
            </tbody>
        </table>
    </div>
  </body>
</html>
